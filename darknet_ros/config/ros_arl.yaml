subscribers:

  camera_reading:
    topic: /camera/image_raw
    queue_size: 1

actions:

  camera_reading:
    name: /ncs_yolo/check_for_objects

publishers:

  object_detector:
    topic: /ncs_yolo/found_object
    queue_size: 1
    latch: false

  bounding_boxes:
    topic: /ncs_yolo/bounding_boxes
    queue_size: 1
    latch: false

  detection_image:
    topic: /ncs_yolo/detection_image
    queue_size: 1
    latch: true

image_view:

  enable_opencv: true
  wait_key_delay: 1
  enable_console_output: true
